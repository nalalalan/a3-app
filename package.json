{
  "name": "chase-aolabs",
  "version": "0.1.0",
  "private": true,
  "description": "Local-only Chase CSV financial radar.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
